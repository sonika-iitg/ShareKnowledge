<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <title>Document</title>
</head>

<body>
  <%- include('../../statics/common/navbar.ejs'); -%>

  <h2 class="my-2">Your Articles</h2>

  <div class="card text-center my-4">
    <% articles.forEach(article=> {%>
    <div class="card-header">
      <div class="row">
        <div class="col-10">
            <h3>
                <%= article.title %>
            </h3>
        </div>
        <div class="col-2">
            ~by
            <%= article.userName %>
        </div>

    </div>
    </div>
    <div class="card-body">
        <p class="card-text"><%= article.discription %></p>
        <a href="/article/<%= article.slug %>" class="btn btn-primary">Read More</a>
        <a href="/article/edit/<%= article.id %>" class="btn btn-primary">Edit</a>
        <form action="/article/<%= article.id %>?_method=DELETE" method="POST" , class="d-inline">
          <button , type="submit" , class="btn btn-danger"> Delete</button>
      </form>
    </div>
    <div class="card-footer text-muted">
        <%= article.creatAt.toLocaleDateString() %>
    </div>
    <% }) %>
</div>

</body>

</html>